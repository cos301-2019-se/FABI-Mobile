language: node_js
  node_js:
    - "8"

  branches:
    only:
      - master
      
  before install:
    - cd 'FABI-WEB'

  before_script:
    - npm install -g @angular/cli

  script:
    - ng build --prod --aot